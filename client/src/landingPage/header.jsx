import BasicTabs from '../basicTab';

export const Header = (props) => {
  return (
    <div class='htmlContents' style={{ paddingTop: 100, height: "auto", minHeight: "550px" }}>
      <BasicTabs />
    </div>
  )
}
